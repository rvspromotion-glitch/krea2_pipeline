# The build needs the handler, the graphs, and the lists that drive the model
# and node installs. Nothing else — git history and tests have no business in
# an image that gets pulled on every cold start.
.git
.github
tests/
README.md
__pycache__/
*.pyc
.pytest_cache/
.venv/
